<%= simple_form_for(@project) do |f| %>
  <%= f.error_notification %>

  <div class="inputs">
    <%= f.input :proj_num %>
    <%= f.input :name %>
    <%= f.input :location %>
  </div>

  <div class="actions">
    <%= f.button :submit %>
  </div>
<% end %>
